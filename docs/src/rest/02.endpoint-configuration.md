# Endpoint Configuration

Endpoint configuration determines which request path Flux.REST uses for each operation in a Set Context.

## Configuration Precedence

When more than one endpoint configuration can apply to the same operation, Flux.REST gives configurations precedence in this order:

- a configuration for a specific operation;
- a configuration for operations of a specific HTTP method;
- a configuration for all operations.

### Example API

To demonstrate endpoint configuration precedence, suppose a school API uses separate paths for retrieving and managing students:

| HTTP method | Request path |
| --- | --- |
| `GET` | `/courses/{courseId}/students?offset={offset}&limit={limit}` |
| `GET` | `/students/{id}` |
| `POST` | `/administration/students` |
| `PUT` | `/administration/students/{id}` |
| `DELETE` | `/administration/students/{id}` |

### Configure Flux

The `Student` Set has three endpoint configurations:

```csharp
.AddSet<Student, int>()
    .WithEndpoint("administration/students")
    .WithGet("students")
    .WithGet((PageRequest _) => "courses/{courseId}/students")
```

- `WithGet("students")` takes precedence over `WithEndpoint("administration/students")` for the Get operation because it uses `GET`.
- `WithGet((PageRequest _) => "courses/{courseId}/students")` takes precedence over `WithGet("students")` because it applies specifically to the Get Page operation.
- For the operations shown here, `WithEndpoint("administration/students")` supplies the path for `Add`, `Update`, and `Remove` operations because no HTTP-method or operation configuration takes precedence for them.

This configuration produces the following request paths:

| HTTP method | Request path | Configuration | Usage |
| --- | --- | --- | --- |
| `GET` | `/courses/{courseId}/students?offset={offset}&limit={limit}` | `.WithGet((PageRequest _) => "courses/{courseId}/students")` | `.GetPageAsync(offset: 0, limit: 20, parameters: parameters)` |
| `GET` | `/students/{id}` | `.WithGet("students")` | `.GetAsync(42)` |
| `POST` | `/administration/students` | `.WithEndpoint("administration/students")` | `.AddAsync(newStudent)` |
| `PUT` | `/administration/students/{id}` | `.WithEndpoint("administration/students")` | `.UpdateAsync(updatedStudent, 42)` |
| `DELETE` | `/administration/students/{id}` | `.WithEndpoint("administration/students")` | `.RemoveAsync(42)` |

> ℹ️ See the other available endpoint configuration overloads.
